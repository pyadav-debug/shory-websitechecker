# Add one table per active partner URL.
# Adding a new partner is just copying the [[urls]] block and changing its values.
# Reduce upcoming_slots as real partners are added so the dashboard keeps the
# intended number of rows.
upcoming_slots = 2

[[urls]]
name = "DLD"
url = "https://www.shory.com/home-insurance/partner/dld"
expected_text = ""
cta_text = ""
allow_status_codes = [200]
